import React from 'react'
import { Card } from 'react-bootstrap'
const WeatherCard = ({ forcast }) => {
  return (
    
    // some card, pass in an object as a prop
    
    <Card>
        
    </Card>
  )
}

export default WeatherCard
